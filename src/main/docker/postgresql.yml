version: '2'
services:
  zima-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/zima/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=zima
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
